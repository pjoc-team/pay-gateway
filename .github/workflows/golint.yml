name: golangci-lint
on:
  push:
  pull_request:
jobs:
  golangci:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: golint
        uses: Jerome1337/golint-action@v1.0.2
        with:
          golint-path: './pkg/...'
